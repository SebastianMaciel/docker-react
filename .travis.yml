# language: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t sebastianmaciel/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true sebastianmaciel/docker-react npm run test -- --coverage
